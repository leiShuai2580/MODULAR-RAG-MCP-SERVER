# 工作流模式

## 顺序工作流

复杂任务应拆成清晰的顺序步骤，并在 `SKILL.md` 开头给 Agent 一个整体流程概览。

示例：

```markdown
填写 PDF 表单包括以下步骤：
1. 分析表单
2. 创建字段映射
3. 校验映射
4. 填写表单
5. 验证输出
```

## 条件工作流

存在分支逻辑时，应引导 Agent 先判断类型，再进入对应流程：

```markdown
1. 判断修改类型：
   创建新内容 -> 使用创建工作流
   编辑已有内容 -> 使用编辑工作流
2. 创建工作流：[步骤]
3. 编辑工作流：[步骤]
```

